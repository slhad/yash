Repository scan and next steps
=============================

Summary
-------
- Found many TODO/FIXME occurrences across the repository. A quick grep returned 1298 matches (truncated to 100 shown).
- Notable locations with references: dist/main.js, src/utils/logger.ts, tests/logger.test.ts, numerous files under node_modules (playwright-core), and git hooks samples.
- Current branch: master
- Working tree: clean (no modified or untracked files at time of scan)

Immediate next steps (recommended)
---------------------------------
1. Prioritise repository-owned TODOs (skip node_modules). Focus on src/ and tests/ first:
   - src/utils/logger.ts: verify LogLevel handling and tests (tests/logger.test.ts) pass.
   - .git/hooks/sendemail-validate: review TODO comments if hooks are enabled in CI.
2. Remove or ignore generated files under dist/ if they are built artifacts. Add/update .gitignore if needed.
3. Run the test suite: `bun test` (this repo uses Bun per AGENTS.md). Fix failing tests.
4. If large numbers of TODOs are technical debt, create issues with examples and assign priorities.

Work items I can take now (choose one):
- Run tests and fix failing test(s).
- Triage TODOs in src/ and create issues for actionable items.
- Clean up dist/ or update build scripts to avoid committing large generated bundles.

Who should do what
-------------------
- I can run tests and open issues / make fixes if you want. Tell me which task to start.

Notes
-----
- The initial grep included matches inside node_modules; those can be ignored unless you plan to modify vendored code.
- The scan was intentionally broad; if you want a focused search (e.g. only src/), tell me and I'll re-run.

Generated by automation on branch: master
