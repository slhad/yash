// Entry point for Yet Another Streamer Helper
import { Dashboard } from './ui/Dashboard';

// In a real implementation, we would:
// 1. Initialize all services (AuthService, ChatService, StreamService, ObsService)
// 2. Initialize platform providers (YouTubeProvider, TwitchProvider, KickProvider)
// 3. Wire up the dependencies
// 4. Render the Dashboard component using OpenTUI

// For now, we'll just log that the application is starting
console.log('Yet Another Streamer Helper starting...');
console.log('To be implemented: TUI with OpenTUI components');

export default Dashboard;
